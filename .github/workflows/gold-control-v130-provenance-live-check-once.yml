name: Gold Control V1.30 Provenance Live Check Once

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/gold-control-v130-provenance-live-check-once.yml'

permissions:
  contents: read

jobs:
  provenance-check:
    uses: ataullahturgut/sim3-automation/.github/workflows/gold-data-neon-ingest.yml@gold-r4-direction-engine
    with:
      mode: hourly
    secrets: inherit
