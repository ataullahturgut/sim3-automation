name: Gold Control V1.27 Deployment Snapshot Final Historical Manual

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-record:
    runs-on: ubuntu-24.04
    steps:
      - name: Historical workflow notice
        run: |
          echo 'V1.27 acceptance is superseded by manifest v1.30.'
          echo 'Its exact executable history and prior Actions evidence remain in Git history.'
          echo 'This workflow is intentionally manual-only and is not a current release gate.'
