# R4.1 — HISTORICAL / NON-CANONICAL

Current project authority is [GOLD_CONTROL_PROJECT_MANIFEST.md](../GOLD_CONTROL_PROJECT_MANIFEST.md).

This directory retains the frozen legacy monthly, FAST/SLOW, Emergency and GVZ context engine used by data-plane adapters and tests. It is not the current research cascade or a separate project authority.

Implementation: `src/gold_r4/`; frozen rules: `config/frozen_r4_1.json`; deterministic tests: `tests/`. From the repository root, install with `python -m pip install -e 'gold_axis_2026/r4_1[test]'` and test with `python -m pytest gold_axis_2026/r4_1/tests`.

For current SQRT-HAR-DR, primary UP V2, UP-2 and failure-filter research implementations, use the exact pinned code links in manifest section 9. Do not substitute this legacy engine for those research identities.
