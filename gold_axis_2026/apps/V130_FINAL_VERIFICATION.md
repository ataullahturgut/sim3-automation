# Gold Control v1.30 Exact-Head Final Verification

This file exists only to trigger the governed v1.30 exact-head final acceptance workflow after fresh production deployment snapshots were deterministically exported and committed.

Expected invariant: committed production snapshots must byte-match fresh read-only Neon exports before any canonical/deployment promotion.
