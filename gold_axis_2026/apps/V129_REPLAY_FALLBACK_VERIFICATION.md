# Gold Control v1.29 Replay Fallback Verification

Exact deployment acceptance must prove that a browser with `NEON_DATABASE_URL` absent renders the persisted September historical replay values from the validated V2 production display snapshot while canonical forecast, selector, ensemble and action authority remain locked.

Final all-gates trigger: replay snapshot V2 + production-backed mobile compatibility on one exact canonical SHA.
