# Gold Control V1.30 — Unresolved Engine Recovery Audit

**Date:** 2026-09-04  
**Status:** FROZEN_RECOVERY_AUDIT_V1  
**Authority:** subordinate to `GOLD_CONTROL_PROJECT_MANIFEST.md` v1.30 and live GitHub/production Neon.  
**Change type:** governance / observability / recovery-state clarification only. No new forecast, selector, ensemble, position mapping, or Decision Store authority is created.

## Frozen global locks

- `AUTO_SELECTOR = OFF`
- `AUTO_ENSEMBLE = OFF`
- `NOT_PROVEN_EXPERT_SELECTION_RULE`
- `NOT_PROVEN_POSITION_MAPPING`
- Historical replay is not prospective evidence.
- A missing original rule is never guessed from literature or fitted retrospectively to archived outputs.

## 1. VW-MIDAS-MSVR

### Recovered

- The audited analytical family and research/rebuild code exist.
- The published 2026 Mineral Economics paper confirms the VW-MIDAS-MSVR family and the broad feature inventory.
- Canonical repository research code itself labels exact replication as blocked and explicitly records unresolved identities/definitions/proxy use.

### Still not recovered / not proven

- exact archived production executable/source chain;
- exact `GPY` identity;
- exact `long_term_trend` construction;
- exact `volatility` construction;
- exact `g_volatility` identity rather than a proxy;
- a complete point-in-time / revision-safe source-vintage contract matching the archived model.

The paper's processed secondary data are not publicly distributed because of source restrictions/proprietary processing; therefore the missing exact project definitions cannot be silently reconstructed from the public article.

**Frozen runtime status:** `BLOCKED_EXACT_REPLICATION_AND_PIT_SOURCE_CONTRACT_NOT_PROVEN`

The existing audited VW result remains an analytical shadow/reference only and must not be relabelled as the exact archived executable.

## 2. Macro Event

### Recovered

The R4.1 frozen material preserves only the partial trigger logic:

- `macro_score <= -1`
- `FAST == DOWN`
- same-day gold return `<= -0.5%`

### Still not recovered

- exact NFP scoring rule;
- exact unemployment scoring rule;
- exact wage/AHE scoring rule;
- exact consensus source and surprise normalization;
- release-vintage identity;
- release timestamp / availability rule required for point-in-time execution.

No exact calculator/source-construction implementation was found in current canonical code or the inspected historical repository tree. A new macro-surprise formula must not be presented as the recovered R4.1 motor.

**Frozen runtime status:** `BLOCKED_EXACT_MACRO_SCORE_CONSENSUS_AND_VINTAGE_CONTRACT_NOT_RECOVERED`

## 3. BOCPD Return

### Recovered from frozen project audit artifacts

Source artifact: `GOLD_REGIME_BOCPD_CUSUM_FINAL_R1.xlsx` and downstream tactical BOCPD audit workbooks.

The following are recovered and must no longer be described as unknown:

- approved detector: `BOCPD-RETURN`;
- role: regime/break alert/context only, never standalone price direction;
- primary series: canonical monthly CORE5 GOLD log returns;
- development window: 2010-01 through 2020-12;
- validation window: 2021-01 through 2023-12;
- locked replay: 2024-01 through 2026-07;
- 2026 tuning: prohibited;
- prior training data window: only history through 2009-12;
- selected expected run length: `L = 36` months;
- frozen development threshold: `0.034027906134261016`;
- adverse break condition: `BOCPD score >= threshold AND completed monthly GOLD log return < 0`;
- signal from month `t` becomes available only after month `t` closes;
- CUSUM-return, BOCPD-volatility and Dual-BOCPD were rejected;
- any 3-month exit overlay was diagnostic only and is not a production action rule.

The workbook also preserves historical BOCPD score, alarm, MAP run length and MAP segment mean outputs through 2026-07.

### Still not recovered

The raw workbook contains no spreadsheet formulas, hidden sheets, VBA, or external-link implementation. Current/historical inspected repository trees contain no exact BOCPD calculator. The remaining missing execution identity is therefore narrower:

- exact predictive likelihood / conjugate-prior parameterization;
- exact initialization of the pre-2010 prior;
- exact definition and implementation of the archived `BOCPD reset score` used to generate the stored scores.

Adams & MacKay defines the general BOCPD framework, but choosing a generic likelihood/prior now would create a new detector, not recover the archived Gold Control implementation. Reverse-fitting parameters to historical outputs is also not accepted as proof of the original executable.

**Frozen runtime status:** `BLOCKED_EXACT_BOCPD_PRIOR_AND_RESET_SCORE_IMPLEMENTATION_NOT_RECOVERED`

Recovered BOCPD constants are evidence for recovery narrowing only; they do not authorize an ACTIVE forward BOCPD engine until an implementation reproduces the frozen score path under the exact project contract.

## 4. Emergency state correction

R4.2 Patch expert reference integration has been validated separately. Emergency Level and Emergency Reversal are therefore no longer blocked by a missing integration bridge. They remain non-voting and wait for the first governed prospective `CAUSAL_PATCH` expert reference.

**Frozen runtime status:** `WAITING_FIRST_GOVERNED_PATCH_EXPERT_REFERENCE`

First eligible reference target remains October 2026 from the completed September month-end origin. No September Patch/Emergency output may be fabricated retrospectively.

## 5. Required runtime/UI state after this audit

Expected operational distribution before any further motor recovery:

- `ACTIVE = 4`
- `WAITING = 5`
- `BLOCKED = 3`
- direction-vote permission is unchanged; this audit grants no new vote.

Blocked motors are exactly:

1. `VW_MIDAS_MSVR`
2. `MACRO_EVENT`
3. `BOCPD`

Waiting motors include Patch, Momentum, Random Walk and the two Emergency components. Historical/current-month replay visibility is a separate evidence layer and must not overwrite operational runtime state.

## 6. Promotion prohibition

This recovery audit does **not** authorize:

- a new VW approximation to inherit the old VW identity;
- a newly designed Macro Event score to inherit the old Macro identity;
- a generic/reverse-fitted BOCPD implementation to inherit the archived BOCPD identity;
- automatic selector/ensemble;
- BUY/SELL/HOLD/EXIT/REDUCE mapping;
- any retrospective claim of prospective issuance.

To remove one of the three remaining blockers, a separate pre-registered change-control must identify either (a) the exact archived implementation/source contract and reproduce frozen evidence, or (b) a clearly new successor motor with a new identity and prospective validation path.